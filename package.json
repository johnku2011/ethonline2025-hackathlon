{
  "name": "pyusd-subscription-platform",
  "version": "1.0.0",
  "private": true,
  "description": "A decentralized subscription platform with PyUSD rewards",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "lint": "pnpm --filter frontend lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "prepare": "husky install"
  },
  "keywords": [
    "ethereum",
    "arbitrum",
    "pyusd",
    "subscription",
    "web3"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^3.0.3"
  }
}
