# Subscription Manager Contract Address (deployed on Arbitrum Sepolia)
SUBSCRIPTION_MANAGER_ADDRESS=0x...

# Backend Private Key (must be the backend address authorized in the contract)
BACKEND_PRIVATE_KEY=0x...

# RPC URL (Arbitrum Sepolia)
RPC_URL=https://sepolia-rollup.arbitrum.io/rpc

# Check interval in milliseconds (default: 3600000 = 1 hour)
CHECK_INTERVAL_MS=3600000
